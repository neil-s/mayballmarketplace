/* global require */
require.config({
	paths: {
		jquery: '../lib/jquery/dist/jquery',
		bootstrap: '../lib/bootstrap/js'
	}
});
require([/* requirements for entry point */], function(/* ... */) {
	// Main entry point
});